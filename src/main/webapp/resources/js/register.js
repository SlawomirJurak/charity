$(document).ready(function () {
    $('#btn-register').click(function (event) {
        registerNewUser($(this), event);
    })
});

function registerNewUser(button, event) {
    
}


function showCommunicate(event, communicate) {
    alert(communicate);
}